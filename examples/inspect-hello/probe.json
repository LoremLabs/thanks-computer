{
  "note": "The inspect inlet lives on the admin API (POST /v1/tenants/{t}/inspect), not the web port, so the smoke harness's HTTP checks don't apply — only the dry-run baseline runs here. Exercise it manually: `txco apply`, then `txco inspect demo user matt@example.com` (see README.md).",
  "checks": []
}
